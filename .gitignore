# Editor / agent metadata
.claude/
.cursor/
.vscode/
.continue/

# Local library and runtime data
/library/
# Backend runtime config (settings.json) lives at the repo root. Anchor the
# rule so it does NOT swallow source dirs named `config/` (e.g.
# frontend/src/config, mobile/lib/core/config).
/config/

# Connector probe scratch output
backend/scripts/toonily_probe_out/

# --- Build & runtime artifacts (platform) ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
node_modules/
.next/
out/
mobile/.dart_tool/
mobile/build/
.venv/
venv/
.env
.env.local
.DS_Store
Thumbs.db
